package com.monday.monday_backend.health.circuitbreaker;

import org.springframework.stereotype.Service;
import org.springframework.web.reactive.function.client.WebClient;

import java.util.concurrent.CompletableFuture;

@Service
public class MondayTokenizationClient {
//    private final WebClient http;
//    private final String url = "";  // Placeholder
//
//    public MondayClient(WebClient.Builder b) {
//        this.http = b.baseUrl(url).build();
//    }
//
//    @CircuitBreaker(name="mondayTokenizationClient", fallbackMethod="fallback")
//    @TimeLimiter(name="mondayTokenizationClient")
//    @Bulkhead(name="mondayTokenizationClient", type=Bulkhead.Type.THREADPOOL)
//    public CompletableFuture<> get

}
